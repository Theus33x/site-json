//Método fetch para buscar o JSON - Banco de Dados

fetch('dados.json')
.then(resposta => resposta.json())
.then(valores => {
    console.log(valores)

    document.getElementById('imagem').innerHTML = valores.imagem;
    document.getElementById('nome').innerHTML = valores.name;
    document.getElementById('estilo').innerHTML = valores.style;
    document.getElementById('preco').innerHTML = valores.price;
    document.getElementById('album').innerHTML = valores.album;

    document.getElementById('imagem2').innerHTML = valores.imagem2;
    document.getElementById('nome2').innerHTML = valores.name2;
    document.getElementById('estilo2').innerHTML = valores.style2;
    document.getElementById('preco2').innerHTML = valores.price2;
    document.getElementById('album2').innerHTML = valores.album2;




});
  







































































































































































































































































































































































































































































































































































